'use strict';

angular.module('navigationBar')
.controller('navBarYesController', [ '$scope', '$window',
    function ($scope, $window) {
/*
		$scope.newDonations = false;
		$scope.$on('thereAreNewDonations', function() {
			$scope.newDonations = true;
		});
*/
    }
]);
